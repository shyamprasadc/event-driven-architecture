{
  "name": "event-driven-ecommerce-nodejs",
  "version": "1.0.0",
  "description": "Event-driven e-commerce platform with microservices architecture",
  "main": "index.js",
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch",
    "lint": "lerna run lint",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "setup": "npm run bootstrap && npm run docker:up"
  },
  "keywords": [
    "event-driven",
    "microservices",
    "ecommerce",
    "nodejs",
    "event-sourcing",
    "cqrs",
    "saga"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^7.1.4",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
